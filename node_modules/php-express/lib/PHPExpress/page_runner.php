<?php\n\n// Le chemin vers votre exécutable PHP\n$phpBin = 'C:\Program Files\PHP\php.exe'; // Remplacez-le par votre chemin PHP si nécessaire\n\n$viewsDir = isset($argv[1]) ? $argv[1] : null;\n$scriptFile = isset($argv[2]) ? $argv[2] : null;\n\nif ($scriptFile) {\n    // Exécutez le fichier PHP\n    include($scriptFile);\n} else {\n    echo 'Aucun fichier PHP spécifié.';\n}\n\n?>
