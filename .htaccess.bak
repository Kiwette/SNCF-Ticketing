AllowOverride All
# Permet d'utiliser les fichiers .htm et .html
Options +MultiViews
AddType application/x-httpd-php .html .htm

# Redirige les requêtes vers un fichier index.php
DirectoryIndex index.php

# Active l'URL rewriting (si nécessaire)
RewriteEngine On
# Si le projet est dans la racine du serveur, retirez RewriteBase /
# RewriteBase /

# Cela redirige toutes les requêtes vers index.php si nécessaire
RewriteRule ^(.*)$ index.php [QSA,L]
